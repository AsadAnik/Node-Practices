const Utils = require('./utils');
const GetNote = require('./notes');

// Using the utils.js module..
console.log(Utils.name);
console.log(Utils.add(5, 10));

// Using the notes.js module..
console.log(GetNote());